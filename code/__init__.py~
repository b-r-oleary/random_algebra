from monkeypatch_stats import *
from monkeypatch_stats import scale, add, offset, posterior, multiply, inverse,\
			      combination, either, order_statistic, min_statistic,\
			      max_statistic, median, mean

from plots import plot_dist
